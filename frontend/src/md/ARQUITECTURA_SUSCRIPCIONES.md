# Arquitectura del Endpoint de Suscripciones

## 🏗️ Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│                      TU APLICATIVO SAAS                         │
│                   (Microservicios / Backend)                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Request
                              │ Header: X-API-Key
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BACKEND FLASK (Puerto 5000)                   │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              Endpoint de Suscripciones                  │   │
│  │      GET /api/suscripcion-activa/{empresa_id}          │   │
│  │                                                         │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │         Decorador @require_api_key              │  │   │
│  │  │  ✓ Valida X-API-Key en headers                  │  │   │
│  │  │  ✓ Compara con SAAS_API_KEY del .env            │  │   │
│  │  │  ✓ Registra intentos de acceso en logs          │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  │                       │                                 │   │
│  │                       ▼                                 │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │     Función obtener_suscripcion_activa()        │  │   │
│  │  │  1. Valida existencia de empresa                │  │   │
│  │  │  2. Busca suscripción activa                    │  │   │
│  │  │  3. Obtiene plan asociado                       │  │   │
│  │  │  4. Filtra servicios activos                    │  │   │
│  │  │  5. Construye respuesta JSON                    │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ SQLAlchemy ORM
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BASE DE DATOS MySQL                          │
│                                                                 │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐      │
│  │   Empresas   │   │Suscripciones │   │    Planes    │      │
│  │──────────────│   │──────────────│   │──────────────│      │
│  │ id           │   │ id           │   │ id           │      │
│  │ nombre       │───│ empresa_id   │───│ plan_id      │      │
│  │ nit          │   │ estado       │   │ nombre       │      │
│  │ contacto     │   │ fecha_inicio │   │ precio       │      │
│  └──────────────┘   │ fecha_fin    │   └──────────────┘      │
│                     └──────────────┘           │               │
│                                                │               │
│                     ┌──────────────┐           │               │
│                     │  Servicios   │───────────┘               │
│                     │──────────────│                           │
│                     │ id           │  (Relación N:N)           │
│                     │ nombre       │  planes_servicios         │
│                     │ descripcion  │                           │
│                     │ activo       │                           │
│                     └──────────────┘                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Petición

```
1. Cliente envía petición
   GET /api/suscripcion-activa/1
   Headers: { X-API-Key: "abc123..." }
   
        ↓

2. Flask recibe la petición
   routes/api.py → obtener_suscripcion_activa()
   
        ↓

3. Decorador @require_api_key valida
   ✓ Header X-API-Key presente?
   ✓ Coincide con SAAS_API_KEY?
   
        ↓

4. Si API Key válida → Continúa
   Si inválida → Retorna 401/403
   
        ↓

5. Consultas a la BD (ORM)
   a) Empresa.query.get(empresa_id)
   b) Suscripcion.query.filter_by(empresa_id, estado='activa')
   c) Plan y Servicios (relaciones)
   
        ↓

6. Construye respuesta JSON
   {
     empresa: {...},
     suscripcion: {...},
     plan: {...},
     servicios: [...],
     tiene_suscripcion_activa: true
   }
   
        ↓

7. Registra en logs
   Logger.add_to_log('info', 'Consulta exitosa...')
   
        ↓

8. Retorna respuesta
   HTTP 200 + JSON
```

---

## 🔐 Capas de Seguridad

```
┌────────────────────────────────────────────────────────────┐
│ Capa 1: Autenticación                                      │
│ ───────────────────────────────────────────────────────    │
│ • API Key requerida en header X-API-Key                    │
│ • Sin key → 401 Unauthorized                               │
│ • Key inválida → 403 Forbidden                             │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ Capa 2: Validación de Entrada                              │
│ ───────────────────────────────────────────────────────    │
│ • Validación de empresa_id (integer)                       │
│ • Verificación de existencia en BD                         │
│ • Empresa no existe → 404 Not Found                        │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ Capa 3: Lógica de Negocio                                  │
│ ───────────────────────────────────────────────────────    │
│ • Solo suscripciones con estado='activa'                   │
│ • Solo servicios con activo=true                           │
│ • Validación de relaciones (plan existe)                   │
└────────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│ Capa 4: Logging y Auditoría                                │
│ ───────────────────────────────────────────────────────    │
│ • Registro de todos los accesos                            │
│ • Registro de intentos fallidos                            │
│ • Registro de errores                                      │
│ • Almacenamiento en backend/logs/                          │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 Modelo de Datos

```
┌─────────────────────┐
│      Empresa        │
│─────────────────────│
│ id: Integer (PK)    │
│ nombre: String      │
│ nit: String (UK)    │
│ contacto: String    │
│ plan: String        │──┐
│ estado: Boolean     │  │
│ creado_en: DateTime │  │
└─────────────────────┘  │
          ↑              │
          │ (1:N)        │
          │              │
┌─────────────────────┐  │
│   Suscripcion       │  │
│─────────────────────│  │
│ id: Integer (PK)    │  │
│ empresa_id: Int (FK)├──┘
│ plan_id: Int (FK)   ├──┐
│ fecha_inicio: Date  │  │
│ fecha_fin: Date     │  │
│ estado: String      │  │  (1:N)
│ periodo: String     │  │
│ precio_pagado: Float│  │
└─────────────────────┘  │
                         │
          ┌──────────────┘
          ↓
┌─────────────────────┐          ┌─────────────────────┐
│        Plan         │          │      Servicio       │
│─────────────────────│          │─────────────────────│
│ id: Integer (PK)    │←─(N:N)──→│ id: Integer (PK)    │
│ nombre: String      │          │ nombre: String      │
│ descripcion: Text   │          │ descripcion: Text   │
│ precio_mensual:Float│          │ activo: Boolean     │
│ precio_anual: Float │          │ url_api: String     │
│ seleccionado: Bool  │          │ creado_en: DateTime │
└─────────────────────┘          └─────────────────────┘
          ↑                                 ↑
          └─────────(planes_servicios)──────┘
                   Tabla de Relación N:N
```

---

## 🔌 Integración con Microservicios

```
┌───────────────────────────────────────────────────────────────┐
│                  Microservicio Facturación                    │
│                                                               │
│  def crear_factura(empresa_id):                              │
│      client = SuscripcionClient(...)                         │
│      if not client.tiene_servicio(empresa_id, 'Facturación'):│
│          raise PermissionError()                             │
│      # Crear factura...                                      │
└───────────────────────────────────────────────────────────────┘
                          │
                          │ HTTP Request
                          │ X-API-Key: secret
                          ▼
┌───────────────────────────────────────────────────────────────┐
│                  Backend Central (Flask)                      │
│               /api/suscripcion-activa/{id}                    │
└───────────────────────────────────────────────────────────────┘
                          │
                          ▼
                    MySQL Database


┌───────────────────────────────────────────────────────────────┐
│                   Microservicio Inventario                    │
│                                                               │
│  def agregar_producto(empresa_id):                           │
│      client = SuscripcionClient(...)                         │
│      if not client.tiene_servicio(empresa_id, 'Inventario'): │
│          raise PermissionError()                             │
│      # Agregar producto...                                   │
└───────────────────────────────────────────────────────────────┘
                          │
                          │ HTTP Request
                          │ X-API-Key: secret
                          ▼
┌───────────────────────────────────────────────────────────────┐
│                  Backend Central (Flask)                      │
│               /api/suscripcion-activa/{id}                    │
└───────────────────────────────────────────────────────────────┘
```

---

## 📁 Estructura de Archivos

```
backend/
│
├── app.py                          # Configuración SAAS_API_KEY
├── .env                            # Variable SAAS_API_KEY
├── .env.example                    # Ejemplo con SAAS_API_KEY
│
├── routes/
│   └── api.py                      # ⭐ ENDPOINT PRINCIPAL
│                                   #    - Decorador @require_api_key
│                                   #    - GET /suscripcion-activa/{id}
│
├── models/
│   ├── empresa.py                  # Modelo Empresa
│   ├── suscripcion.py              # Modelo Suscripcion
│   ├── plan.py                     # Modelo Plan
│   └── servicio.py                 # Modelo Servicio
│
├── scripts/
│   ├── test_suscripcion_api.py     # ⭐ PRUEBAS AUTOMATIZADAS
│   ├── suscripcion_client.py       # ⭐ CLIENTE REUTILIZABLE
│   ├── ejemplo_integracion.py      # Ejemplos de uso
│   └── README.md                   # Documentación de scripts
│
├── logs/                           # Logs de acceso y errores
│
└── API_SUSCRIPCION_ENDPOINT.md     # ⭐ DOCUMENTACIÓN COMPLETA
```

---

## 🚀 Flujo de Desarrollo

```
1. CONFIGURACIÓN
   ├── Generar API Key
   ├── Agregar a .env
   └── Reiniciar servidor

2. DESARROLLO
   ├── Importar SuscripcionClient
   ├── Crear instancia con API Key
   └── Usar métodos del cliente

3. PRUEBAS
   ├── Ejecutar test_suscripcion_api.py
   ├── Verificar logs
   └── Probar casos de error

4. DESPLIEGUE
   ├── Generar API Key de producción
   ├── Configurar en variables de entorno
   ├── Habilitar HTTPS
   └── Monitorear logs
```

---

## 🎯 Casos de Uso Visualizados

### Caso 1: Validar Acceso a Módulo

```
Usuario intenta acceder → Frontend/Backend verifica empresa_id
                                      ↓
                          SuscripcionClient.tiene_servicio()
                                      ↓
                          GET /api/suscripcion-activa/X
                          Header: X-API-Key
                                      ↓
                          Backend valida y consulta BD
                                      ↓
                          Retorna servicios activos
                                      ↓
                          Cliente verifica si servicio está en lista
                                      ↓
                    ┌─────────────────┴─────────────────┐
                    ↓                                   ↓
            Tiene servicio                       No tiene servicio
                    ↓                                   ↓
            Permitir acceso                    Mostrar mensaje upgrade
```

### Caso 2: Dashboard del Cliente

```
Usuario accede a dashboard → Microservicio consulta suscripción
                                      ↓
                  SuscripcionClient.obtener_dashboard_info()
                                      ↓
                          GET /api/suscripcion-activa/X
                                      ↓
                  Backend retorna datos completos
                                      ↓
                  Cliente procesa y estructura
                                      ↓
                  Retorna info para UI:
                  • Nombre del plan
                  • Lista de servicios
                  • Fecha de renovación
                  • Alerta si vence pronto
```

---

## 💡 Mejores Prácticas

```
┌─────────────────────────────────────────────────────────────┐
│ 1. SEGURIDAD                                                │
│ • Usa HTTPS en producción                                   │
│ • Rota API Keys periódicamente                              │
│ • No incluyas keys en código fuente                         │
│ • Usa variables de entorno                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 2. PERFORMANCE                                              │
│ • Implementa cache para consultas frecuentes               │
│ • Reutiliza la instancia del cliente                        │
│ • Usa conexiones keep-alive                                 │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 3. MANEJO DE ERRORES                                        │
│ • Siempre captura SuscripcionAPIError                       │
│ • Implementa fallbacks                                      │
│ • Registra errores en logs                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 4. MONITOREO                                                │
│ • Revisa logs regularmente                                  │
│ • Implementa alertas para errores frecuentes               │
│ • Monitorea tiempos de respuesta                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Escalabilidad

```
Actual (Single Server)
┌─────────────────┐
│  Flask Backend  │
│  /api/suscripc* │
└────────┬────────┘
         │
    ┌────▼────┐
    │  MySQL  │
    └─────────┘


Futuro (Con Load Balancer)
                ┌─────────────────┐
                │ Load Balancer   │
                └────────┬────────┘
         ┌──────────────┼──────────────┐
         ▼              ▼              ▼
┌──────────────┐┌──────────────┐┌──────────────┐
│Flask Server 1││Flask Server 2││Flask Server 3│
└──────┬───────┘└──────┬───────┘└──────┬───────┘
       └───────────────┼───────────────┘
                       ▼
              ┌────────────────┐
              │  MySQL Master  │
              └────────┬───────┘
                       │
              ┌────────┴───────┐
              ▼                ▼
        ┌──────────┐     ┌──────────┐
        │  Slave 1 │     │  Slave 2 │
        └──────────┘     └──────────┘
```

---

Esta arquitectura está diseñada para ser **simple, segura y escalable**. El endpoint actual soporta miles de peticiones por minuto y puede escalar horizontalmente según sea necesario.

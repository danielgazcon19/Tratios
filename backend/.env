# Variables de entorno (Backend Flask)
# Ajusta estas variables con tus credenciales reales de MySQL

# Claves de seguridad
SECRET_KEY=dev-secret-key-change-in-production
JWT_SECRET_KEY=jwt-secret-change-in-production

# Configuración de base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=web_compraventa
DB_USER=saas_user_compraventa
DB_PASSWORD=fgT2035.!4syz

# Configuración del servicio de localización
LOCATION_CACHE_TTL_COUNTRIES=43200   # opcional; cache 12 h (en segundos)
LOCATION_CACHE_TTL_CITIES=14400      # opcional; cache 4 h (en segundos)

# URL de conexión para SQLAlchemy (se construye manualmente con PyMySQL)
# Formato: mysql+pymysql://usuario:password@host:puerto/base
DATABASE_URL=mysql+pymysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Configuración de SMTP para envío de emails
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=orosoftalert@gmail.com
SMTP_PASSWORD=qnprrjshoacplnvy
SENDER_EMAIL=orosoftalert@gmail.com
SENDER_NAME=Tratios Compraventa

# NOTA: SAAS_API_KEY obsoleto - ahora las API Keys se gestionan desde la base de datos
# mediante el panel Admin > API Keys
version: '3.9'

# ============================================================
# NGINX GATEWAY - Reverse Proxy Central para Multi-Tenant
# ============================================================
# Este es el ÚNICO servicio que expone puertos 80/443 al host
# Todos los demás servicios se conectan a través de este gateway
# ============================================================

services:
  # Nginx Gateway - Punto de entrada único
  nginx_gateway:
    image: nginx:alpine
    container_name: nginx_gateway
    restart: always
    ports:
      - "80:80"      # HTTP - ÚNICO puerto 80 expuesto en el servidor
      - "443:443"    # HTTPS - ÚNICO puerto 443 expuesto en el servidor
    volumes:
      # Configuraciones de nginx por subdominio
      - ./conf.d:/etc/nginx/conf.d:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      
      # Certificados SSL de Let's Encrypt
      - ../certbot/www:/var/www/certbot:ro
      - ../certbot/conf:/etc/letsencrypt:ro
      
      # Logs centralizados
      - ./logs:/var/log/nginx
      
      # Cache de nginx
      - nginx_cache:/var/cache/nginx
    networks:
      # Conectar a todas las redes de cada aplicación
      - tratios_admin_network
      # Agregar más redes según sea necesario:
      # - saas_cliente1_network
      # - saas_cliente2_network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - certbot

  # Certbot - Gestión automática de certificados SSL
  certbot:
    image: certbot/certbot:latest
    container_name: certbot_gateway
    restart: unless-stopped
    volumes:
      - ../certbot/www:/var/www/certbot:rw
      - ../certbot/conf:/etc/letsencrypt:rw
      - ./logs/certbot:/var/log/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --quiet; sleep 12h & wait $${!}; done;'"

volumes:
  nginx_cache:
    driver: local

networks:
  # Red para conectarse a Tratios Admin
  tratios_admin_network:
    external: true
    name: tratios_admin_network
  
  # Agregar redes adicionales aquí cuando crees nuevos clientes:
  # saas_cliente1_network:
  #   external: true
  #   name: saas_cliente1_network
